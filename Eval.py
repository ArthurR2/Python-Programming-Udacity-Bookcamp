# We can also interpret user input as a
# Python expression using the built-in
# function eval. This function evaluates a
# string as a line of Python.
result = eval(input("Enter an expression: "))
print(result)
